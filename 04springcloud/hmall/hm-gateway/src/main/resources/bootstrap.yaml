spring:
  application:
    name: hm-gateway
  profiles:
    active: dev
  cloud:
    nacos:
      server-addr: 192.168.157.128:8848
      config:
        file-extension: yaml
        shared-configs:
          - dataId: shared-log.yaml # 共享日志配置
